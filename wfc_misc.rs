use crate::misc:: {
    index_to_uvec3,
    udiv_up_safe32,
    uvec3_to_index
};

pub fn test_data() -> {
    let result: Vec<[f32 ; 3]> = Vec::new();
    for i in -2..2 {
        for k in -2..3 {
            
        }
    }
    result.push([i, 0, k]); 
}
